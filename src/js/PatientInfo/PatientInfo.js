import React, { Component } from 'react';
import { connect } from 'react-redux';

class PatientInfo extends Component {
  render() {
    return <div></div>;
  }
}

export default connect(PatientInfo);
